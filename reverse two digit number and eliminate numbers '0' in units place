#reverse two digit number and the nummber with '0' in units place is not printed
x=int(input("enter two digit number:"))
if x>10 and x<100:
	if x%10!=0:
		i=x%10
		rev_num =i*10+x//10
		print(rev_num)
